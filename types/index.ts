import type {Link} from "./ui";
import type {TWalletCookie, LockedNFT, Contribution, PresaleContribution, SaleState, Token} from "./data";

type SaleType = "presale" | "sale";

export type { Link, TWalletCookie, LockedNFT, Contribution, PresaleContribution, SaleState, Token, SaleType }